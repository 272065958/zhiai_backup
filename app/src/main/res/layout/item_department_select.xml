<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/department_select_icon"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/tab_height"
        android:src="@drawable/choice_orange" />

    <TextView
        android:id="@+id/department_name"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/tab_height"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:textColor="@color/text_main_color"
        android:textSize="@dimen/text_size_middle" />

    <View
        android:id="@+id/department_bottom_line"
        android:layout_width="match_parent"
        android:layout_height="@dimen/divider_height"
        android:layout_alignParentBottom="true"
        android:background="@color/divider_color"
        android:visibility="gone" />

    <View
        android:id="@+id/department_bottom_divider_line"
        android:layout_width="match_parent"
        android:layout_height="@dimen/divider_height"
        android:layout_alignParentBottom="true"
        android:layout_marginLeft="@dimen/auto_margin"
        android:layout_marginRight="@dimen/auto_margin"
        android:background="@color/divider_color"
        android:visibility="gone" />

    <View
        android:id="@+id/department_right_line"
        android:layout_width="@dimen/divider_height"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:background="@color/divider_color"
        android:visibility="gone" />
</RelativeLayout>